import { Route } from '@angular/router';

export const appRoutes: Route[] = [
            {
                path:'CRM', children: [
                //{ path: 'Login', loadChildren: () => import('app/modules/General/Pagina-Inicial/Pagina-Inicial.routes') }
                ]
            }
        ];